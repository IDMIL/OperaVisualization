// Overwritten by the "Set last updated date" step in .github/workflows/deploy.yml
// on every deploy build, so this value always reflects the most recent commit
// that made it to production rather than when this file itself was edited.
export const LAST_UPDATED = "2026-09-01";
